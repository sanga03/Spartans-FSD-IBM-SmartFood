spring:
  cloud:
    config:
      uri: http://172.18.3.44:8762
  application:
    name: custom-food-details-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: null
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
server:
  port: 9000